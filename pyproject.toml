[project]
name = "dl-project-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cristian Bastias", email = "cristian.bastias@simpro.cl" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
dl-project-template = "dl_project_template:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pandera>=0.33.1",
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]
